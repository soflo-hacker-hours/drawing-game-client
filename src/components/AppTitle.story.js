import React from 'react'
import AppTitle from './AppTitle'

export default { title: 'Components/App Title', component: AppTitle }

export const Default = () => (
  <div>
    <AppTitle label="Some Title" />
  </div>
)
